# AGENTS.md

本仓库是一个用于政府采购招标文件合规性审查的 LLM Wiki。

## 架构

采用 Karpathy 风格的 LLM Wiki 分层：

- `raw/`：不可变的原始材料。招标文件、法规、截图、PDF、Word 文档和抽取后的纯文本都放这里。
- `wiki/`：可由智能体编辑的 Markdown 知识库，是项目的长期工作记忆。
- `AGENTS.md`：智能体操作协议、笔记结构和工作约束。
- `wiki/index.md`：知识库地图。
- `wiki/log.md`：仅追加的入库、整理、审查日志。
- `tools/wiki.py`：本地维护工具，支持 `ingest`、`query`、`lint`。
- `ingest`、`query`、`lint`：对 `tools/wiki.py` 的轻量命令封装。

## 智能体协作协议

处理招标文件时：

1. 不得编辑或覆盖 `raw/` 中的原始文件。
2. 标准审查必须采用“AI 审查”口径：只使用本项目 `raw/`、`wiki/`、`tools/` 中的材料和明确引用的法规依据，不得从外部目录、历史批注、标准答案、V2 检查结果或人工标注结果中反推风险点。
3. 外部批注、标准答案或人工标注只能用于 AI 审查完成后的精确率提升流程，形成独立复盘记录；不得生成第二套项目审查记录，也不得混入标准审查记录冒充 LLM Wiki 自审结果。
4. 先依据 `wiki/20-知识点/政府采购招标文件画像.md` 和 `wiki/60-提示词/招标文件画像提示词.md` 建立政府采购招标文件画像。
5. 再依据 `wiki/20-知识点/政府采购逐章审查矩阵.md` 逐章抽取条款、识别缺失项和匹配风险。
6. 将相关条款抽取到 `wiki/40-审查工作台/审查记录/` 下的项目审查记录，项目记录默认且仅为 `AI 审查版`。
7. 然后对照 `wiki/25-风险审查点/`、`wiki/20-知识点/` 和 `wiki/30-风险库/` 匹配风险。
8. 每个风险点必须包含：
   - 招标文件中的原文证据；
   - 风险判断；
   - 关联风险审查点；
   - 关联法规、规则、知识点或风险模式；
   - 风险等级；
   - 具体修改建议。
9. 法律依据不确定时，标记为 `需进一步确认`。
10. 审查中发现的可复用知识，必须沉淀回 `wiki/20-知识点/`、`wiki/25-风险审查点/` 或 `wiki/30-风险库/`。
11. 重要的入库、整理和审查工作，要记录到 `wiki/log.md`。

## 命令

在仓库根目录运行：

```bash
./ingest raw/招标文件/example.pdf
./query "本地化"
./lint
```

也可以直接使用等价的 Python 入口：

```bash
python3 tools/wiki.py ingest raw/招标文件/example.pdf
python3 tools/wiki.py query "本地化"
python3 tools/wiki.py lint
```

## Markdown 结构

使用兼容 Obsidian 的 Markdown。wiki 笔记顶部优先使用这些字段：

```text
类型::
主题::
状态::
来源::
风险类型::
风险等级::
审查状态::
关联法规::
关联知识点::
```

## 链接规则

- 内部链接使用 Obsidian 双链：`[[wiki/20-知识点/资格条件审查]]`。
- 外部官方来源使用 Markdown 链接。
- 没有原文摘录时，不得生成风险结论。
- 除非必要，不粘贴长篇法规原文；优先使用条文编号加官方来源链接。

## 操作语义

### Ingest

`ingest` 不只是建索引。对每个来源文件，智能体应当：

1. 在 `wiki/50-语料与摘录/` 下创建或更新来源笔记；
2. 抽取关键事实和证据；
3. 更新相关法规、知识点、风险和审查记录页面；
4. 更新 `wiki/index.md`；
5. 向 `wiki/log.md` 追加可解析的日志条目。

本地 `./ingest` 脚本只完成机械的第一步。智能体仍负责阅读来源文件，并把知识整合进整个 wiki。

### Query

`query` 从检索 wiki 开始，然后阅读相关页面，并结合 wiki 页面和原文摘录给出可追溯回答。若回答形成可复用分析，应沉淀回 wiki。

### Lint

`lint` 应同时检查机械结构健康和知识库质量：

- 缺失 `AGENTS.md`、`wiki/index.md` 或 `wiki/log.md`；
- 损坏的 wiki 双链；
- 页面顶部缺少元数据；
- 页面未加入索引；
- 过时、矛盾或缺少证据支持的结论；
- 孤立页面和缺失的交叉引用；
- 应当独立成页的重要概念。

## 审查分类

主要风险族：

- 限制排斥供应商
- 指向性参数
- 评分因素不当
- 业绩奖项限制
- 本地化限制
- 政府采购政策适用错误
- 合同条款不清或显失公平
